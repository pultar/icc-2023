#!/usr/bin/env python

import setuptools  # type:ignore

setuptools.setup(
    name="icctools",
    version="0.0.1",
    maintainer="Felix Pultar",
    python_requires=">=3.9",
    install_requires=[],
    packages=["icctools"],
)
